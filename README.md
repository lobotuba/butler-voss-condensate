# Butler-Voss Condensate

A physics-**inspired** research model (Robert Voss; formerly the "Grain Fabric
Model"). Space is treated as an **active medium** sampled by a network of
**nodes** — a node is a physical locus that carries field state, not a bare graph
vertex, and its edges are real tension-bearing relations. Disturbances propagate
as ripples; nonlinearity self-traps them into localized "particles"; and the
medium's own structure is the candidate source of charge, spin, and gravity.

> A toy model, not a claim about the real universe. Every idea here is turned
> into a **measurement** so it can be confirmed or refuted from the numbers.

The conceptual stance (built up across the work below): the medium — the
"condensate" — is the substance; nodes sample/excite it; **charge is a
topological winding**, **spin is a Noether charge**; the localized structures sit
**a layer below quantum particles** (which would be their bound states); and
**gravity is meant to emerge from the medium**, not be inserted.

## Status at a glance

| | Hypothesis | Result |
|---|---|---|
| **H1** | focusing makes persistent particles | ✅ supported (oscillon lives full run; control disperses) |
| **H2** | gravity via tension-advection | ❌ not supported (leaky / disruptive) |
| **H3** | a particle is a moving pattern | 🟡 weak (moves in the push direction) |
| **H4** | results independent of lattice | ⚠️ artifact-sensitive (`square2d` CFL; `cubic3d` anisotropy) |
| **H5** | 2D vs 3D differ | ✅ both host oscillons; differ as expected |
| **H6** | charge = topological winding | ✅ confirmed (integer, conserved) |
| **H7** | spin = Noether charge | ✅ confirmed (Q-ball) |
| **H8** | only ~3–5 fundamentals | ✅ *derived*: `n=0, ±1` + Noether |
| **H9** | higher layers by confluence | ✅ opposite bind, `n=2` splits, scale gap ≈6.6× |
| **H10** | self-cohering medium | ✅ self-assembles to isotropic close packing, sets spacing |
| **P0–P1** | field on a self-assembled medium | ✅ charge survives (needs LSQ operator) |
| **P2** | field on a *moving* medium | ✅ charge survives reconnections; lost at melting |
| **P3** | gravity-by-density | ✅ **two masses attract** (3d): energy-conserving variational coupling makes two lumps drift together (sep 9.3→7.5), no attraction at β=0, stronger at higher β. Halves validated (3a/3b), coupling energy-conserving (bounded `g`). No single-lump self-bound soliton (medium nearly incompressible → gravity is real but weak) |

## The three bodies of work

### 1. Base engine — H1–H5 (`simulation.py`)
The original scalar-field engine: a displacement field on fixed lattices
(`hex2d`, `square2d`, `cubic3d`, `fcc3d`), with oscillon particles, tension, a
tension-advection gravity attempt, and particle tracking → CSV.

```bash
python simulation.py --live --experiment lump                 # watch an oscillon breathe
python simulation.py --headless --experiment collide --steps 3000
python simulation.py --headless --experiment lump --damping 1 --gravity 0   # energy-conservation check
```
Key findings: focusing produces persistent particles (H1); the tension-advection
gravity term mostly drains energy (H2); `cubic3d` is artifact-prone while
isotropic lattices (`hex2d`, `fcc3d`) are stable (H4).

### 2. Layer-below program — H6–H10 (three prototypes)
Promotes the field to a complex **order parameter**, so the medium can carry the
quantum numbers, and makes the lattice itself dynamical.

```bash
python prototype_complex.py       # H6–H8: charge = winding, spin = Noether; species census
python h9_binding.py              # H9: opposite charges bind, n=2 splits, scale gap
python prototype_mobile_nodes.py  # H10: mutually-attracting nodes self-assemble to a lattice
```
Findings: charge is an integer topological winding (H6) and spin a Noether charge
(H7); the stable species reduce to `n=0, ±1` plus the Noether family (H8);
opposite charges bind into composites while like charges repel (H9); and a cloud
of mutually-attracting nodes self-selects an isotropic close-packed lattice,
*setting* its own spacing (H10) — which is why a self-organizing medium avoids the
`cubic3d` artifact entirely.

### 3. Integration — Phases 0–2 (field on the self-assembled medium)
Puts the complex field **on** the mobile medium (field attached to nodes).

```bash
python integration_field_medium.py   # Phase 0–1: meshfree operator accuracy; vortex on a frozen medium
python integration_phase2.py         # Phase 2: vortex on a MOVING, rearranging medium
```
Findings: an irregular mesh **requires a least-squares meshfree Laplacian** (the
plain graph operator degrades ~16×) (P0); a vortex's **topological charge is
conserved** on a self-assembled medium (P1) and survives the medium *moving and
reconnecting* under it (~308 reconnections), failing only when the medium
**melts** (P2). The core shape is under-resolved on the irregular mesh — an open
quality issue, not a topological one.

### Phase 3 — gravity-by-density (✅ two masses attract)
Close the loop two-way: field energy compresses the medium → denser nodes slow
the local waves → other excitations refract toward the mass = attraction. The
non-leaky replacement for H2's tension-advection gravity.

```bash
python integration_phase3ab.py          # 3a refraction (sign) + 3b compression, in isolation
python integration_phase3c.py           # close the loop on one lump
python integration_phase3_variational.py # both couplings from one energy functional (conserving)
python integration_phase3d.py           # THE gravity test: two masses drift together
```
The arc: **3a/3b** validated the two halves and the sign (waves refract *toward* a
dense region). **3c** closed the loop on one lump (self-focuses but leaks with the
hand-tuned coupling). The **variational** form derives both couplings from one
energy functional (energy-conserving by construction); a bounded `g(ρ)` then cured
a blow-up and fixed conservation. A single lump does **not** self-bind — the medium
is nearly incompressible, so gravity-by-density is real but *weak*. But self-binding
isn't needed: **3d shows two masses drift together** (separation 9.3 → 7.5),
with **no attraction at β=0** and stronger attraction at higher β, energy conserved
— gravitational attraction emerging from the medium.

## Install

```bash
pip install -r requirements.txt    # numpy, matplotlib
```
Requires Python 3.11+.

## File map

| File | Purpose |
|---|---|
| `bvc_core.py` | Shared primitives: LJ medium + self-assembly, meshfree operators, lattice builders |
| `simulation.py` | Base scalar engine — lattices, dynamics, particle tracking, CLI (H1–H5) |
| `prototype_complex.py` | Complex order parameter; topological + Noether charge; species census (H6–H8) |
| `h9_binding.py` | Binding / confluence study (H9) |
| `prototype_mobile_nodes.py` | Self-cohering medium of mobile nodes (H10) |
| `integration_field_medium.py` | Field on a self-assembled medium; meshfree operators (Phases 0–1) |
| `integration_phase2.py` | Field on a moving, rearranging medium (Phase 2) |
| `integration_phase3ab.py` | Gravity-by-density, the two halves in isolation (Phases 3a/3b) |
| `integration_phase3c.py` | Gravity-by-density, closed loop on one lump (Phase 3c) |
| `integration_phase3_variational.py` | Gravity-by-density from one energy functional (energy-conserving) |
| `integration_phase3d.py` | The gravity test: two masses drift together (Phase 3d) |
| `operator_scorecard.py` | Symmetric field-operator candidates scored (symmetry/stability/accuracy) |
| `figures.py` | Renders the headline results to `figures/` (self-assembly, vortex/charge, gravity) |
| `interaction_energy.py` | Quantifies the gravitational contact strength: the interaction well `U(d)` |
| `density_response_3d.py` | 3D (ordered fcc) density response, small medium (3D-0/3D-1) |
| `density_response_3d_large.py` | 3D sparse scale-up (3D-2): gravity is screened (λ≈3.3), not Newtonian |
| `screening_diagnosis.py` | Screen-0: coupling strength β is not a lever — screening is intrinsic |
| `screening_gauss.py` | Screen-1: existence proof — the medium carries unscreened 1/r iff the mediating field is massless (screening = a mass term) |
| `screening_topocharge.py` | Screen-2: a conserved topological charge sources a long-range (2D-Coulomb log) force — the model hosts EM-like *and* nuclear-like forces |
| `screening_topocharge_3d.py` | The 3D topological defect (Route A): long-range survives into 3D as a vortex-line interaction (log per unit length); vortex ring is a closed 3D line (E = tension × circumference) |
| `route_b_hedgehog.py` | Route B: the S² hedgehog — a genuine 3D **point** topological charge (integer); bare self-energy diverges linearly (global monopole); the pair interaction is short-ranged, not 1/r |
| `route_c_monopole.py` | Route C: the gauged monopole (compact U(1), Coulomb phase) — **EM-in-3D**: finite deconfined self-energy and a genuine 1/r² Coulomb field between quantized topological point charges |
| `test_lorentz.py` | Test for emergent Lorentz invariance: field-sector isotropy emerges (LV ~ (E/E_Planck)²⁻⁴), but field and medium have different light cones (speed non-universality — the central obstacle) |
| `test_lorentz_unified.py` | Unified prototype: governing medium + field by one vector-Hooke operator gives a single universal light cone (c_L = c_T = c_field exactly), recovering speed universality |
| `test_lorentz_boost.py` | Emergent boost invariance: the massless cone and massive mass-shell are boost-invariant at low energy (violation ~ (E/E_Planck)²), signals sub-luminal — the full Lorentz group emerges |
| `test_dirac.py` | Emergent relativistic fermions: a honeycomb medium gives a linear isotropic Dirac cone (v_F=3/2), but fermions come as opposite-chirality pairs (Nielsen–Ninomiya); the triangular close-packing has no cone |
| `test_domain_wall.py` | Evading the chirality wall: a Wilson–Dirac (Chern) strip binds a single chiral fermion to each edge (opposite chirality, spatially separated) — the domain-wall route to Standard-Model chirality |
| `test_quantization.py` | First quantization: the unified sector quantizes to a relativistic QFT (quanta on the mass-shell; massless→power-law, massive→Yukawa vacuum correlator). Honest: it imposes QM, doesn't derive it |
| `test_graviton.py` | The sharpest barrier: long-range spin-2 gravity — diagnosed, not achieved. Massless mediator would give universal 1/r attraction, but the model's mass-coupling is screened, and the medium has no spin-2 (only spin-0/1 phonons) |
| `test_fracton_gravity.py` | Route 1 (elasticity–fracton duality): the medium's defects source a biharmonic tensor-gauge theory — curvature (disclinations) is long-range while energy/dilatation is screened. ⚠️ **Its conclusion that this "overcomes the screening" is RETRACTED** — see `test_disclination_force.py` |
| `test_graviton_spin2.py` | The spin-2 half: the tensor-gauge field is a genuine graviton — exactly 2 transverse-traceless polarizations carrying helicity ±2 (vs a photon's ±1), universal 1/r² attraction, light-bending factor 2 |
| `test_cone_universality.py` | Honest correction: the fermion cone `v_F` is **not** locked to the boson cone `c_B` (`v_F/c_B = √3·t/√(K/m)`, a tuning not a symmetry) — emergent Lorentz is a within-sector result |
| `test_cone_lock.py` | The cure: a *composite* boson (particle–hole / induced gauge field) rides the fermion light cone — `ω_min(q) → v_F\|q\|` — so cross-statistics Lorentz universality is automatic when all excitations descend from one structure |
| `test_induced_action.py` | Sakharov's lock: integrating out the fermions induces a Lorentz-invariant boson action — the polarization depends on `(q,Ω)` only through `Ω²+v_F²q²` (0.10% spread) — the emergent gauge field inherits the fermion cone |
| `test_emergent_tetrad.py` | **Capstone:** the medium's own bond fluctuations *are* the emergent photon (Dirac-node shift, spin-1) and graviton (cone deformation / tetrad, spin-2) — both read off the fermion dispersion, so both ride one cone |
| `test_lv_prediction.py` | **Frontier 1 — first falsifiable prediction:** quadratic (n=2), crystallographically-anisotropic, cross-species-universal Lorentz violation (`ζ~0.2`, `E_QG,2~2.5×10¹⁹ GeV`) — consistent with all bounds, falsifiable in structure |
| `test_graviton_dynamics.py` | **Frontier 2 — emergent gravity, running:** the graviton propagates as a gravitational wave (group velocity 0.97c, massless), rides the one universal cone (`ζ=0.25`), and mediates a universal `1/r²` attraction |
| `test_induced_gravity.py` | **Frontier 2 completion — Sakharov:** the graviton's Einstein–Hilbert kinetic term is *generated* by the fermion stress-tensor loop `⟨TT⟩` (Lorentz-invariant, spread → 0.37% as s→0) — its dynamics inherited from the fermion cone, not imposed |
| `test_emergent_qm.py` | **Frontier 3, step 1:** the wave half of QM emerges — the field's NR envelope spreads at the exact Schrödinger rate (`D` measured 0.830 vs predicted 0.833), with `ħ/2m = c²/2Ω` a material property of the medium. The Born rule / measurement remain the open moonshot |
| `test_born_rule.py` | **Frontier 3, step 2:** the Born rule as a stochastic equilibrium (Nelson/Valentini) — an ensemble started non-Born relaxes to `\|ψ\|²` (KL `4.4→1e-4`) under the medium's diffusion `ν=ħ/2m`; `\|ψ\|²` is an attractor, not a postulate |
| `test_double_solution.py` | **Frontier 3, step 3:** de Broglie's double solution — deriving guidance `v=∇S/m`. A soliton carrying its *own* phase drifts at exactly `k` (slope **1.000**): `λ=h/p` is a theorem of the medium. But a resting soliton is *not* steered by a *separate* pilot wave (slope **≈0**): guidance-by-a-distinct-wave and definite outcomes stay a postulate — the honest F3 boundary |
| `test_shielding.py` | **Why gravity is unshieldable.** Screening is *not* loss (a superconductor screens `B` with zero dissipation) — it is **neutralization**, and mass is unipolar. Gauss-law probe: a **dilatation** charge is cancelled exactly by the medium (gate `div u ≡ C·s` to `7.5e-16`) ⇒ shieldable ⇒ short-range; a **topological** charge is **exactly invariant** (`+1.000000000`) under any smooth response, stiffness contrast, or relaxation. Topological quantization does what "no negative mass" does in nature |
| `test_tetrad_shielding.py` | The tetrad graviton is **long-range but shieldable**. Bitter–Crum kills the strain's **trace**, not its **shear** — the deviator falls as `r^-2.01` (box-gated), and the tetrad *is* the traceless part, so it reads the one unscreened sector. But an intervening shell **attenuates it ~4×** — real gravity shows none. Diagnosis: the medium's moduli are a *free* background; in GR stiffness *is* mass and can only add |
| `test_disclination_force.py` | **Route 1's force law — it fails.** Real-space clamped disc (no periodic IR saturation), box-gated. Control reproduces the known dislocation log-repulsion. Measurement: two **like disclinations REPEL**, `\|dE\| ~ R^1.97` — wrong **sign** (gravity attracts) and wrong **direction** (force *grows* with R). A disclination is a **charge**, not a **mass**. ⚠️ **Reinterpreted, and its open problem solved:** the growing energy is **confinement** (a constant-force string tension in 3D), and the induced Einstein term *deconfines* it into an exact Newtonian `−1/r` with two healthy propagating polarizations — the very construction this row called the honest open problem (`test_deconfinement`, `test_spin2_dynamical`) |
| `test_light_bending.py` | **First assault on the spin-2 wall: `γ = 0`, derived.** Does the model bend light? The PPN `γ` decides it (0 = scalar, 1 = GR's factor of two). Measured `γ = 1.4×10⁻⁵ ≈ 0`, and the *reason* is the result: a mass can only make the medium move its nodes (a displacement `u`), and displacement-derived strain is **compatible** → a **flat** metric → the coordinate change `x→x+u` → **pure gauge**, which bends no light (`η/strain = 1.8×10⁻¹⁵`). Genuine curvature exists **only** as *incompatible* strain — a disclination (`η = 0.13`) — but that sector **repels** (`test_disclination_force`). So the wall is structural: a fixed-background medium responds to mass by a diffeomorphism, and cannot make GR's spatial curvature. The route through is a metric dynamical in its own right, not a finer lattice. ⚠️ **Conclusion superseded:** γ=0 holds for the *compatible/scalar* sector (still true, still load-bearing), but the curvature sector it dismissed as repulsive is in fact **confining**, and *deconfines* into an attractive massless graviton with **γ→1** (`test_deconfinement`, `test_induced_sign`, `test_spin2_dynamical`) |
| `test_incompatible_gravity.py` | **The door through the wall: the graviton lives in incompatible bond DOF.** The wall tested only node displacements. But each triangular-lattice site has 3 bond lengths vs a displacement's 2 DOF → bond fluctuations = 2 (displacement, gauge) + **1 incompatible (curvature)**. Shown: the compatible sector is flat (`η=1.8×10⁻¹⁵`) and bends nothing, but the incompatible sector (mass → curvature `η~ρ`, via an Airy potential, gate `η==ρ` to `6×10⁻¹⁰`) **does deflect light**, long-range, vs the compatible sector's exact zero. Reframes the frontier from "can it bend light" (yes, in this sector) to "is the coupling **Einstein** (`γ=1`)" |
| `test_two_gravities.py` | **Which of the model's two gravities wins at long range.** The model has a **scalar** gravity (amplitude mode, γ=0, the working force of `test_critical_gravity`) and a **tensor** gravity (graviton, γ=1). Which dominates at long range is set by **mass**, not coupling (massless `1/r` beats massive Yukawa). So if the graviton is massless and the amplitude mode gapped, **γ(r) climbs 0→1** across the amplitude Compton wavelength — a falsifiable *scale-dependent γ* (short-range deviation from GR). **Reinterpretation:** `test_critical_gravity`'s "gravity" was the scalar *tuned* long-range (`m_A→0`), which forces γ=½ (ruled out) — the wrong move; the amplitude mode should stay gapped. **Crux:** the spin-2 sector is *confining* in the pure medium, so the hurdle is to **deconfine it into a massless graviton** (turn `test_disclination_force`'s `+R` into `−1/r`) |
| `test_deconfinement.py` | **Deconfining the spin-2 sector — `+R` → `−1/r`, given `μ>0`.** The confining sector is the **biharmonic** `κ∇⁴` (in 3D `G=r/8πκ` → constant force = a string tension `s²/8πκ`). The Sakharov-induced Einstein term adds `μ∇²`; the full operator `κ∇⁴−μ∇²` has the exact closed form `G(r)=(1/4πμr)(1−e^{−r/ℓ})`, `ℓ=√(κ/μ)`. **Measured with the correct tool — the *radial ODE*** (a point mass is spherically symmetric, so `u=rG` gives a 1-D tridiagonal solve on an effectively infinite line: **no box, no images**; and by linearity the two-body force is exactly `−s²G'(R)`, so one radial solve gives the whole force law). Result, clean to machine precision: the Newton tail slope is **−2.0000**, `G=1/(4πμ)` exact to **5 figures**, the closed-form match is **~10⁻⁷** (grid-gated `∝h²`), and — what no finite box can show (Ewald tail; walls cap the IR) — the μ=0 **confinement growth `G∝r^{+1.0000}`** with a constant force = tension. So the lower-derivative induced term dominates the IR and **deconfines** the graviton. Honest scope: assumes the induced `μ>0`; its **sign** is *measured* in `test_induced_sign` (positive) — this row supplies the mechanism, that one the crux |
| `test_induced_sign.py` | **The sign of the induced gravitational coupling: `μ>0`, measured.** `test_deconfinement` left tensor gravity resting on one number's sign — does the fermion loop induce a *positive* (healthy) Newtonian kinetic term? This is the Sakharov sign, notoriously scheme-sensitive. **The convention-free handle: calibrate against the model's own working sector.** The induced *photon* is healthy (`test_induced_action`), so its induced Coulomb term — the charge-density correlator `⟨J₀J₀⟩` — is a healthy dielectric (`χ>0`). Computing, *from the same gapped-Dirac loop with identical conventions*, the induced *Newtonian* term — the energy-density correlator `⟨T₀₀T₀₀⟩` (energy = the gravitational charge) — its `q²` coefficient comes out the **same (positive) sign** as `⟨J₀J₀⟩` in **all 15 cases** (5 masses × 3 cutoffs), for both energy-density vertex definitions. So the induced gravity is as healthy as the induced electromagnetism the model already runs on: **`μ>0`**. By `test_deconfinement` this deconfines the confining `+R` into an attractive `−1/r`. Honest scope: only the **sign** is robust (the magnitude of `G` is UV/cutoff-dependent — the Sakharov feature); this is the `h₀₀`/Newtonian (force) sector on the 2+1D cone, where the spatial spin-2 graviton is non-dynamical (`q²` coef `~0`), so the full `γ=1` light-bending completion stays the open `test_graviton_ward` item. What is settled is the deconfinement input |
| `test_spin2_dynamical.py` | **The induced spin-2 graviton is dynamical & healthy in 3+1D — the piece 2+1D can't have.** `test_induced_sign` found the *spatial* graviton non-dynamical on the 2+1D cone — kinematics, not failure: a massless symmetric tensor has `D(D−3)/2` polarizations = **0** in 2+1D, **2** in 3+1D. So the radiative, light-bending, `γ=1` spin-2 sector exists only in 3+1D. Going there (4-component Dirac loop, projector bubble, `q∥z` so the TT modes are transverse): **[A] dynamical** — the TT kinetic `q²` coefficient is `~10⁻⁴` (≈0) in 2+1D but `−0.069` in 3+1D; **[B] spin-2** — the two polarizations `h₊(xx−yy)` and `h×(xy)` are **degenerate** to 4 digits (one helicity-±2 field, not two unrelated modes); **[C] healthy** — same sign as the induced *transverse photon* (Maxwell), the model's working gauge field (both spatial vertices, so calibration is convention-free). Robust across mass and cutoff. With `test_induced_sign`'s healthy `h₀₀` (`μ>0`), **both** graviton sectors — Newtonian force and radiative spin-2 — are induced and healthy in the physical dimension. `γ=1` then follows by Weinberg (massless spin-2 + conserved IR stress tensor → Einstein); this supplies the previously-missing *measured* fact that the spin-2 graviton actually propagates. Direct transversality (`q_iΠ=0`) stays the regulator-limited `test_graviton_ward` item; `G`'s magnitude stays cutoff-dependent (Sakharov) |
| `test_lattice_ward.py` | **The symmetry-preserving regulator: photon Ward identity exact, γ=1 emergent — resolving `test_graviton_ward` structurally.** That earlier test found the graviton transversality `q_iΠ=0` (which forces γ=1) regulator-limited — a hard cutoff breaks the Ward identity via a surface term — and flagged the fix as "a lattice with an *exact* lattice Ward identity." This builds it: a Wilson-Dirac model on a periodic **BZ torus** (no boundary → no surface term) with the **diamagnetic seagull**. Result, a sharp dichotomy: the **photon** (U(1), an *exact* lattice symmetry via Peierls) closes to **machine precision** — `Π^{xx}_para + K^{xx} = 0` to `10⁻¹⁶` at every q, while the transverse Maxwell response stays nonzero (a real cancellation, not a trivial zero) → **the regulator works**. The **graviton** (diffeomorphism, *not* a lattice symmetry — only discrete translations) cannot: its Ward identity is **inhomogeneous** (`⟨T^{xx}⟩=−0.33≠0`, the induced stress/cosmological term) and the longitudinal stress response doesn't cancel. So γ=1 is **not** a lattice-exact identity and no finite-cutoff *direct* measurement can make it one — `test_graviton_ward`'s "regulator-limited" is **structural, not a bug**. γ=1 is therefore correctly an **IR-emergent** statement (same footing as emergent Lorentz): Weinberg's theorem on the conserved IR Dirac stress tensor, with the spin-2 graviton now measured to propagate and be healthy (`test_spin2_dynamical`). The loop is closed |
| `test_cosmological_constant.py` | **Why the medium's huge vacuum energy doesn't gravitate — the CC fine-tuning, dissolved.** The zero-point energy is `~1/a₀⁴ = M_Planck⁴`, `~10¹²²×` observed dark energy — naively "the worst prediction in physics." But the model's vacuum **is a self-sustained condensate** (`test_collapse`), and (Volovik, emergent gravity in quantum liquids) the emergent metric couples to the vacuum **stress** — the grand potential `ρ_Λ = ε−μn = −P` (exactly `test_lattice_ward`'s `⟨T⟩~−P`) — **not** the bare `ε`. A self-sustained vacuum has `P=0`, so the density **self-adjusts** to make `ρ_Λ=0` to machine precision for bare `ε₀` swept across **all 122 orders**, with **no parameter tuning** (a rigid vacuum would gravitate the full `ε₀` — control shown). The residual scales as the fractional departure `δ` from equilibrium (`ρ_Λ~δ`), so the observed tiny Λ measures how close the vacuum sits to equilibrium. **Honest scope:** makes the *equilibrium* CC exactly zero and automatic (dissolving the tuning); does **not** derive the observed nonzero value — relocated to why the vacuum is slightly off equilibrium (expansion/matter/relaxation), still open |
| `test_yang_mills.py` | **Emergent non-Abelian gauge fields: the fermion loop induces Yang-Mills, not just photons.** `test_induced_action` induced the U(1) photon's Maxwell term; the SM needs `SU(2)`/`SU(3)`. The decisive distinction is the **self-interaction**: a non-Abelian field strength `F = dA + i[A,A]`, so a *uniform* non-Abelian field has `F=i[A_x,A_y]≠0` from the commutator alone, while a uniform Abelian field is always pure gauge. Put a fermion in the fundamental of SU(N) with uniform links: **commuting** (Cartan) config → induced action `~10⁻¹⁵` (**machine zero, pure gauge**); **non-commuting** config → `E−E₀ ∝ A^3.97 ≈ A⁴` = the Yang-Mills `Tr[A_x,A_y]²`. N²−1 decoupled photons would give zero for *both*. Shown for **SU(2)** (3 bosons) and **SU(3)** (8 gluons, color), with a universal induced `1/g²` guaranteed by exact non-Abelian lattice gauge invariance (Wilson links — same exact-symmetry footing as `test_lattice_ward`'s U(1)). Emergent gauge theory scales from U(1) to SU(N) by the same Sakharov mechanism. **Honest scope:** the *mechanism*, not the SM — it does **not** derive the group `SU(3)×SU(2)×U(1)`, chiral coupling, fermion reps/hypercharges, anomaly cancellation, or the Higgs. The group is an input; its Yang-Mills dynamics are induced |
| `test_anomaly_inflow.py` | **Chirality without inconsistency: a quantized anomaly, supplied by bulk inflow.** `test_domain_wall` gave a single chiral fermion; `test_yang_mills` gave non-Abelian gauge fields. The SM needs both at once — and there's the real obstruction: `SU(2)_L` couples **chirally**, and a chiral gauge theory is *inconsistent* unless its anomalies cancel. The domain wall survives by **Callan–Harvey anomaly inflow**, and the content is quantized: `(bulk Chern C) = (chiral modes per wall) = (charge pumped per flux quantum)` — one integer. Measured on the QWZ strip: topological phase gives `C = −0.9995`, edge spectral flow **+1** on one wall and **−1** on the other, **summing to exactly 0**; trivial control gives `0, 0, 0`. So each wall is *individually anomalous* (its gauge charge is not conserved), the lattice as a whole is vector-like and anomaly-free (as Nielsen–Ninomiya requires), and the charge a wall loses is pumped through the **bulk** to the other wall. **Honest ceiling:** this is *not* the SM's anomaly cancellation — the SM is a standalone 4D chiral theory cancelling among its **own** content (the quark/lepton hypercharge conspiracy, `ΣY=0`, `ΣY³=0`), with no bulk to lean on. Here the bulk does the cancelling. Chirality is shown **consistent**; the SM's particular chiral content remains an input |
| `test_realtime_pump.py` | **The first *dynamical* integration: chiral matter + a gauge field in one running simulation.** Every prior result lives in a band structure or defect algebra — never a running system where two emergent sectors interact in time (the project's own sharpest self-criticism, §10). `test_anomaly_inflow` proved Callan–Harvey inflow *statically*, by counting. This threads **one flux quantum** through the QWZ cylinder and evolves the filled sea under the **time-dependent Schrödinger equation** (exact exponentiation each step, no adiabatic-following shortcut). Result: **ΔQ = +0.9991** of charge physically crosses between the walls in the topological phase, **exactly 0** in the trivial control — and the adiabatic gate converges (`err` 2.5×10⁻³→8.6×10⁻⁴ as the ramp slows, `~1/N_t`). The walls are spatially separated, so the charge went through the **bulk** — the anomaly observed as *dynamics*, not inferred from topology. **Honest scope:** the threaded field is the **U(1) gauge** field, not gravity; the full integration §10 asks for — chiral quantum matter interacting through emergent **spin-2 gravity with back-reaction** — is *not* done. What's shown: the first pair of emergent sectors runs together, and the topological accounting survives contact with real dynamics |
| `test_backreaction.py` | **Gravitational back-reaction, run as a conserving simulation** — the step that makes this a *scientific* simulation of gravity, not a toy. Gravity's defining feature is back-reaction (matter curves geometry, geometry moves matter, solved *together*), and this evolves the **Schrödinger–Newton** system — self-gravitating quantum matter — using the derived IR gravity (Newtonian, `G=1/4πμ`, `μ>0` from `test_deconfinement`/`test_induced_sign`). Four things a toy fails, all passed: **[A] conservation** — total energy to `3×10⁻⁹`, norm to `4×10⁻¹⁴` through the full nonlinear evolution (vs the ~80%-leaking Phase-3 artifact); **[B] self-binding** — gravity ON forms a bound soliton (`E<0`, width stays finite), gravity OFF disperses (2.45→5.02) — the self-bound lump the near-incompressible Phase-3 medium *couldn't* make; **[C] virial equilibrium** — the relaxed soliton satisfies `2T+W=−0.03≈0` (requires **isolated** free-space BC via Hockney; a periodic box gives a spurious +0.95, shown as a methodological check); **[D] convergence** — `E` and virial settle under grid refinement (N=24→32→40). **Honest scope:** non-relativistic (Schrödinger, not Dirac matter), scalar/Newtonian (the `h₀₀` sector; the radiative spin-2 graviton is not evolved), classical field. The full target — chiral quantum matter through emergent spin-2 gravity with radiative back-reaction — stays open. What's closed: gravity here is a **force that can be run**, self-consistently and without leaking, and it makes the bound states self-gravity is meant to make |
| `test_newton_constant.py` | **The magnitude of `G`: order the Planck area, no tuning** (resolving the "cutoff-dependent `G`" caveat). Newton's constant is `G=1/(4πμ)`, `μ`=the induced Newtonian stiffness (`q²` coefficient of `⟨T₀₀T₀₀⟩`). The Sakharov caveat — `μ` is UV-dominated, so `G` depends on an arbitrary cutoff — *doesn't apply here*: the cutoff is the **physical** lattice scale `1/a₀` (`a₀=l_Planck`, `test_scale_fixing`). Computed over the **full** 3+1D lattice BZ, `μ` is an O(1) number in lattice units, so **`G = O(1)·a₀² = O(1)·l_Planck²`** — the Planck area up to an order-unity factor, computed not fitted (`μ≈0.17`→`G≈0.46 a₀²` for one fermion). The `N_f` scaling is standard Sakharov (`G~a₀²/N_f`; an SM-like count gives `~0.01 a₀²`, still Planckian). So gravity is weak because `a₀` is Planckian — no hierarchy, no tuning. **Honest scope:** the *scale* (order `a₀²`) and *sign* (positive/attractive) are robust; the precise O(1) coefficient is scheme-sensitive (residual Sakharov ambiguity) |
| `test_gravitational_radiation.py` | **Gravity that radiates — and a monopole that can't.** `test_backreaction` ran gravity in the *Newtonian* limit (instantaneous constraint). A real gravitational field is **retarded** and carries energy away. Evolving the linearised TT wave equation `∂²_t h^TT + k²h^TT = S^TT` with the exact TT projector: **[A] propagation** — after a short burst the shell travels at **speed 0.958 ≈ c** (retarded, not instantaneous); **[B] two polarizations** — the TT projector has **rank 2**, the helicity-±2 states of `test_spin2_dynamical`, no scalar breathing mode; **[C] the discriminator** — at identical amplitude, width and frequency a **monopole radiates `1.05×10⁻¹⁴`** against the **quadrupole's `3.79×10⁻²`** (ratio `3×10⁻¹³`, i.e. machine zero). Scalar (Nordström) gravity *would* radiate breathing waves from a pulsating star; **spin-2 gravity forbids it** — a sharp structural confirmation that the model's gravity is genuinely tensor, not the scalar it discarded. **Honest scope:** linearised, with a **prescribed** source — back-reaction of the radiated energy *on* the source (binary inspiral) is not computed, and the quadrupole **luminosity law is not tested** (it assumes a source small vs the wavelength; this one isn't, so the frequency dependence here reflects the source's spatial spectrum, and no such claim is made). ✅ **Now tested and reproduced by `test_quadrupole_luminosity`, which also quantifies why it failed here** (`ωσ≈1.6` → power suppressed ~13×) |
| `test_quadrupole_luminosity.py` | **Einstein's quadrupole luminosity formula, coefficient and all — the gravity arc's first HARD NUMBER.** Every prior result was *structural* (a sign, a rank, a scaling, a prohibition, a balance): strong, but none could be **contradicted by a known closed-form answer**. This one can be, and isn't. The prediction `L = g²/(160π)·Q⃛_ij Q⃛_ij` is **derived from the project's own field normalisation, not fitted**. **[0] the derivation, audited before simulating** — the chain reproduces GR's binary law `(32/5)Gμ²a⁴Ω⁶` **exactly**, and the TT angular average `∮dΩ = (8π/5)Q·Q` to `1.7×10⁻⁵`. **[A] the identity it all rests on** — `∫T_ij d³x = ½Ï_ij` holds to `5.2×10⁻⁸` for a rotating body carrying its **full** stress: ram pressure **plus the centripetal binding stress** that holds a spinning body together (omit that term and the identity fails outright — the classic way this calculation goes wrong). This is *why* the **mass** quadrupole, not the stress, sets the radiation. **[B] the coefficient** — measured/predicted = **0.9924 / 0.9983 / 0.9996**, with `E_f(t)` linear to `0.99998`; because a *rigidly* rotating quadrupole has a **constant** near-zone energy, the slope **is** the radiated power, so nothing is fitted but the line. Stable to `~10⁻³` over a 4× range of `dt` (non-monotonic, so it *bounds* the integrator's contribution rather than extrapolating it away). **[C] the scaling laws** — **`L ∝ Ω^6.007`** (quadrupole radiation demands exactly 6; this is precisely the claim `test_gravitational_radiation` had to drop when it came out `Ω^−3.96`) and `L ∝ M2^2.000`. **[D] why the earlier attempt couldn't have worked** — the σ sweep tracks the exact Gaussian form factor `exp(−ω²σ²)` to four decimals across a **12× suppression**, so the old failure at `ωσ≈1.6` (~13× down) is now *quantified rather than excused*. **Honest scope:** the source is **prescribed**, not self-gravitating — deliberate, since back-reaction is a higher-order effect that would contaminate a leading-order measurement, and prescription was never the obstacle (**compactness was**). What is tested is the radiation **law**. ✅ **Its dynamical consequence — a bound binary actually decaying — is now tested by `test_inspiral_peters`** |
| `test_inspiral_peters.py` | **Orbital decay vs Peters–Mathews — the Hulse–Taylor observable, and the arc's second hard number.** `test_quadrupole_luminosity` reproduced a *rate*; this tests the **consequence** of that rate for a bound system, against a **second independent closed form** (Peters 1964 — the law confirmed on the binary pulsar, 1993 Nobel). The test has teeth because **GR has only one Newton constant**: the binding sector (`G_N=g_N/4π`) and the radiative sector (`G_rad=g²/32π`, fixed in §8.23) are **locked** by `g_N = g²/8`, so orbital frequency, binding energy and radiated power all follow from the *same* `G` — **nothing is tunable per-quantity**. **[0] chain audited in closed form before simulating** — `(G/5)Q⃛·Q⃛ = (64/5)G⁴m⁵/a⁵` exactly, and energy balance reproduces `da/dt = −(128/5)G³m³/a³` to `3.7×10⁻¹⁶`. **[A] grid luminosity of a real Keplerian binary vs Peters, across separation** — ratios **0.9903 / 0.9924 / 0.9939 / 0.9973**, and crucially the ratio **trends to 1 as `v/c` falls** (0.160→0.131), exactly as a leading-order result must; fitted exponent **`L ∝ a^−4.983`** (Peters: exactly −5) — the `a⁻⁵` steepening that makes an inspiral run away. **[B] the decay rate `da/dt` vs Peters** — same 0.99 ratios. **[C] the chirp** — integrating gives the closed-form `(t_c−t)^{1/4}` trajectory to coalescence at the Peters time. **Honest scope:** radiation reaction is **adiabatic** (grid-measured `L` fed through energy balance, exactly how binary inspirals are modelled in GW astronomy), **not** a first-principles self-force. The rate is measured at **fixed separation deliberately** — a field-energy budget over the *moving* orbit conflates radiation with **changing near-zone standing energy (~15%)**, a confound called out rather than papered over. Newtonian point-mass orbit, leading quadrupole, `v/c≈0.13–0.16`, so percent-level agreement is the right expectation, not machine precision |
| `test_radiative_backreaction.py` | **The integration closed: a source that radiates and thereby DECAYS.** Three pieces existed separately — self-consistent matter+gravity but *non-radiative* (`test_backreaction`), a radiative field but from a *prescribed* source (`test_gravitational_radiation`), and two sectors run together (`test_realtime_pump`). This couples all three: matter and the radiative TT field evolve from a **single Hamiltonian**, so radiation reaction is *derived, not inserted*. **[A] the budget closes** — matter energy falls `2.0451×10⁻²` while field energy rises `2.0452×10⁻²`, balancing to `8×10⁻⁶` with the total conserved to `2×10⁻⁶`; **[B] monotonic decay** — an inspiral in field-theoretic form; **[C] control** — a spherical source radiates `5×10⁻⁸`, a factor **4×10⁵** smaller: the monopole prohibition survives being made self-consistent, so it wasn't an artifact of prescribing the source; **[D] it's the coupling** — `E_rad/g²` is flat to 4 digits (`6.783, 6.784, 6.785 ×10⁻⁵`), so the transfer tracks the gravitational coupling, not numerical leakage. **Honest scope:** the matter is a **classical, non-relativistic** field (not the relativistic quantum chiral matter of the fermion sector) and the gravity is **linearised**; `g` is dialled far above physical so the decay is visible in a short run — the *mechanism and budget* are demonstrated, not the magnitude. ✅ **All three caveats since addressed one at a time by `test_relativistic_backreaction`** |
| `test_relativistic_backreaction.py` | **Past the toy: relativistic quantum matter, nonlinear gravity, the coupling taken to zero.** Attacks the three admitted caveats above *separately*, so each upgrade is measured rather than bundled. **[A] relativistic + quantum** — the Schrödinger field becomes a genuine **Dirac** field (4-component spinor, first-order, `α/β` matrices, spin and antiparticle components) carried as a **many-fermion Slater determinant** of 4 occupied modes; because the gravitational coupling is a *one-body* operator, determinant evolution is **exact for the matter** (no mean-field error there). The budget closes to `8.6×10⁻⁹` with the total conserved to `2×10⁻¹⁰`, and **Pauli survives** — the modes stay orthonormal to `5×10⁻¹²`. A spherical control radiates **2×10⁸ times less**, sitting *at* the integrator's noise floor, so the monopole prohibition holds for relativistic quantum matter too. **[B] nonlinear** — the field gains the derivative self-coupling that is GR's structural signature (`(λ/2) h_kl ∂_k h_ij ∂_l h_ij`, gravity gravitates). Energy is still conserved to `1.4×10⁻¹¹`, so it is a genuine **Hamiltonian** term and not a patch; and decisively **superposition FAILS** — evolving two packets together stops equalling evolving them apart: `5.7×10⁻¹⁶` (machine zero) at `λ=0`, rising to `2.1×10⁻³` at `λ·amp=0.96`, tracking the vertex strength. Waves that scatter off each other are not a linear field. **[C] the coupling** — `E_rad/g²` is flat to **six significant figures across four decades of `g`**, so the transfer is *exactly* second order and extrapolating to physical strength is arithmetic, not assumption. The honest floor is reported: the closure test stays *verifiable* only down to `g~10⁻³`, where the transfer meets the integrator's drift; below that it is extrapolation and is labelled one. Physical coupling (`~10⁻⁴⁰` in this ratio) is **not reachable in double precision by any direct simulation**. **[D] all three on** — the self-interaction carries **9.5%** of the field energy and shifts the radiated energy by **2.0%** (genuinely active, not decoration), and the budget still closes to five figures. **Still open, and not weakened by any of this: the GEOMETRY IS CLASSICAL** — this is *semiclassical* gravity (quantum matter, classical field), so the measurement problem is untouched; and the cubic vertex is the structural nonlinearity, **not** the resummed Einstein–Hilbert series. There is no black hole here and none is claimed. ✅ **That classical-geometry gap is now measured, not just named, by `test_semiclassical_inconsistency`** |
| `test_semiclassical_inconsistency.py` | **What the classical geometry costs — semiclassical gravity is INCONSISTENT, measured from the inside (honest negative).** `test_relativistic_backreaction` named "the geometry is classical" as the remaining gap and left it as an asterisk. It isn't one: the prescription used throughout the gravity arc is **demonstrably wrong, not merely approximate**. **[A] nonlinear in the WAVE FUNCTION** — sourcing gravity on the matter density `ψ*ψ` breaks the superposition principle: residual `2.8×10⁻¹⁵` (machine zero) with gravity off, **`4.6×10⁻¹`** with it on. Note the contrast with §8.22, where a nonlinear *field* was the **point** (GR has one); here the nonlinear object is the *wave function*, and that's a **defect**. **[B] a single particle attracts ITSELF** — one particle (norm 1) in a two-place superposition: the lumps close `4.979→4.879` while the gravity-off control *drifts apart* to `5.014`. Reading the force straight off the field (so dispersion can't confound it), it equals the Newtonian pull of a point mass of **half the particle's own mass** to ratio **1.000** at separations 7 and 9 — *the full force of a partner that does not exist*. **[C] the case needing no interpretation (Page–Geilker 1981)** — a **classical** coin flip puts a mass at L or R; branch-wise force on a central test mass is `∓0.77608`, semiclassical sources the ensemble average and gives **`−0.00000`**. It predicts the test mass **stays put when it must fall**, and because the randomness is classical, *no interpretation of QM is available to rescue it*. **[D] the model's own way out** — `h` is a **collective mode of a quantum condensate**, and collective modes of quantum media are quantised (phonons are), so the semiclassical treatment was a tractability approximation, *not a claim about nature*. Quantising one mode **entangles matter with geometry** and decoheres it (`S>0`, purity`<1`), matching the exactly solvable answer to `~10⁻¹⁶`, while the semiclassical column sits at coherence `1.000000` **forever** — a classical field sourced on `⟨σ_z⟩=0` never moves, so it can never entangle or decohere anything. **Honest limit:** [D] is **linearised** quantum gravity — the *easy, long-known* part (perturbative QG as an effective theory). It is **not** quantum geometry, says nothing about the nonperturbative problem, and does **not** solve the measurement problem: it relocates branch structure into entanglement *without selecting an outcome*. Single-mode coherence recurs at `t=2π` (correct for one mode; irreversible decoherence needs a continuum) — flagged so the table isn't over-read |
| `test_experimental_bounds.py` | **The model against the data — and one retraction.** Confronts the two live predictions with real bounds. **[A] n=2 Lorentz violation:** safe against every current limit by **~16 orders in the effect** — but that cuts both ways, it is **not currently falsifiable** (needs ~8 orders in quadratic-dispersion sensitivity). An honest tempering: a quadratic Planck-suppressed signal is out of reach; current experiments probe the *linear* n=1 signal the model does **not** predict. **[B] Scale-dependent γ — the real testable one:** a massless graviton plus a gapped amplitude mode gives a Yukawa addition of range `1/m_A`, so `γ<1` inside it. Cassini is weak (`λ<1.4×10¹⁰ m`); **short-range torsion-balance tests bind, beating Cassini by 14 orders** → `m_A ≳ 3.9 meV`, within a factor **1.6 of the dark-energy scale** (2.4 meV) — exactly the sub-mm window these experiments are pushing into. **[C] Retracts `test_scale_fixing`'s 10¹²² criticality tuning:** that assumed gravity's range *is* `1/m_A`; the tensor arc replaced it, leaving only a *lower* bound an untuned medium clears by ~30 orders. With the cosmological-constant result, the model has now shed **both** its 10¹²² fine-tunings — each by a structural result, not a fitted parameter |
| `test_graviton_ward.py` | **Graviton Ward identity — γ=1 argued, not yet cleanly measured (honest negative).** Whether the induced graviton is transverse (`q_iΠ^{ij,kl}=0`) forces γ=1 (Weinberg). The direct numerical check is **regulator-limited**: even the *current* (photon) validation, which charge conservation requires to be exactly transverse, won't fall cleanly to zero (cutoff surface term; PV/soft cutoffs don't fix it), so the stress ratio isn't trustworthy. What stands is the **analytic** argument on established results: IR sector = Lorentz-invariant Dirac (`test_lorentz`/`test_dirac`) → conserved stress tensor → transverse spin-2 (Weinberg) → **γ=1**. The wall is crossed *in principle* by the IR fixed point; a clean in-model measurement (needs a symmetry-preserving regulator) remains open. ✅ **Now explained, not merely open:** `test_lattice_ward` built that regulator — the photon Ward identity closes to machine precision, but the graviton's *cannot* close exactly because diffeomorphism invariance is not a lattice symmetry and its Ward identity is inhomogeneous. γ=1 is **structurally** an IR-emergent identity. ⚠️ **And `test_ir_fixed_point` narrows what "IR fixed point" may be leaned on for:** it is *empirical, not protected*, and γ=1 remains argued |
| `test_ir_fixed_point.py` | **Auditing the project's largest claim — "GR is reached as an IR fixed point" — and narrowing it to what is measurable.** Flagged as the biggest vulnerability: not necessarily wrong, but an extraordinarily large claim carrying little support. The specific problem: **"fixed point" is a renormalisation-group term and no RG analysis existed anywhere in the project** — no operator classified relevant/irrelevant, nothing said about what the IR forgets. The phrase borrowed authority from a calculation never done. This does it, from the **exact closed-form** Green's function of the deconfined sector with a graviton mass added — `G(r) = [e^{−√B r} − e^{−√A r}]/(4πκ(A−B)r)`, `A+B=μ/κ`, `AB=μm²/κ` — with **no box, no lattice, no periodic images**. **[A] universality** — varying `κ` over **four decades** at fixed `μ`, probing each at equal depth into its *own* far field, the far-field law is Newton to **ten figures** with exponent `−2` to five: `κ` sets only *where* the crossover sits, never what lies beyond, and the residual falls as `e^{−r/ℓ}` — the IR forgets the UV **exponentially**, not as a power. **[B] the operator sorting** — relevance is a statement about the `r→∞` *limit*, so each deformation is tracked outward: higher-derivative structure dies as `e^{−r/ℓ}` and is gone by `100ℓ` however large `κ` (**irrelevant**), while a graviton mass `10⁻⁴` of the Einstein scale **grows** outward (`1.0×10⁻³ → 3.0×10⁻³ → 1.0×10⁻²`) until the tail is destroyed (**relevant**). **[C] the bound** — since everything rests on that relevant operator being absent, the measured tail excludes any induced graviton mass above **~3×10⁻⁵** of the Einstein scale (Compton wavelength `≳10⁴ℓ`). **Verdict: the claim is narrowed.** *Earned:* the **linearised** Einstein term is the IR **attractor**, as a real RG statement. *Not earned:* the fixed point is **empirical, not protected** — diffeomorphism invariance is not an exact lattice symmetry, so nothing *forbids* the relevant deformation; masslessness is **measured, not guaranteed**, unlike the photon's symmetry-protected masslessness. The analysis is **linearised** and reaches neither the nonlinear Einstein equations nor a measurement of `γ=1`. Wording corrected throughout the whitepaper from *"general relativity as a fixed point"* to *"the linearised Einstein term as an IR attractor"*. ✅ **The lack of protection is then *measured* by `test_graviton_mass`** |
| `test_graviton_mass.py` | **Is a graviton mass induced? Testing the assumption the whole gravity arc rests on.** `test_ir_fixed_point` showed a mass is *the* relevant deformation — so the attractor stands or falls on its absence — but it only bounded a mass **it had inserted by hand**. The propagator used everywhere else, `κ∇⁴−μ∇²`, **has no mass term written in it**: masslessness has been an **assumption of the operator form**, never a measurement. `test_induced_sign` makes the omission explicit — it extracts `μ` from `Π(q)−Π(0)` and **discards `Π(0)` as a contact term**, and *that discarded number is the mass candidate*. **Method chosen so no perturbative bookkeeping can hide the answer:** instead of assembling a bubble plus a seagull and hoping the set is complete, compute the filled-sea energy under a **constant** deformation — that *is* the `q→0` limit, and contains every order at once. **[A] photon control** — constant gauge field gives `d²E/dA² = −1.5×10⁻¹⁰`, improving under refinement (`1.9e-7 → 5.6e-10` as `N: 24→48`); zero **because a symmetry forbids it**. **[B] tetrad graviton** — constant traceless cone shear `v=(1+ε,1−ε,1)` gives **`−0.27`**: order unity, **eight orders** above the control, stable across gap and grid. **The sign matters and is not the naive expectation:** it is **negative**, so shear *lowers* the sea energy — the loop doesn't merely give the cone a mass, it **destabilises the symmetric cone**, and the medium's own shear rigidity must overcome it. Either sign settles the question: the `q=0` term is O(1) and nothing forbids it. **Consistent, not fatal** — a tetrad mode with an O(1) `q=0` term is *not a massless long-range mediator*, which is exactly why `test_tetrad_force` found **no** long-range force from the tetrad's `1/r²` field and why the elastic route was declared dead (Eshelby–Crum); two unrelated calculations agree, and the negative sign explains why that route could never have been rescued by tuning. **What it does *not* show:** the gravity the project claims is the **deconfined curvature sector**, not the tetrad, and its masslessness rests on the biharmonic structure rather than any symmetry protecting the cone shape — **not measured here**. The assumption is **narrowed, not removed**: masslessness *cannot be inherited from the tetrad*, so the curvature sector must supply it alone. **That was the sharpest load-bearing assumption remaining — and `test_curvature_mass` then closed it** |
| `test_curvature_mass.py` | **The curvature sector's q=0 mass IS the cosmological constant — and it vanishes.** The decisive test. `test_graviton_mass` showed masslessness **cannot be inherited from the tetrad**, leaving the deconfined curvature sector's `q=0` term as the last load-bearing assumption. It turns out **not to be a separate assumption at all.** `test_cosmological_constant` already said so in passing — *"Π(0,0) is the induced cosmological piece, and ⟨T^ij⟩≠0 is the same vacuum stress"* — and the algebra is elementary: a vacuum energy enters as `√g Λ`, and `√g = 1 + ½tr h + ⅛(tr h)² − ¼tr(h²) + O(h³)`, so the piece **quadratic in h carries no derivatives** — which is precisely what a mass term is: `S ⊃ −(Λ/8)[2h_ij h_ij − (tr h)²]`. For transverse-traceless `h` that is `−(Λ/4)h_ij h_ij`, **a mass for the propagating spin-2 modes themselves**, so **`m² ∝ Λ` exactly**. **[A] identification verified, not asserted** — numerical expansion matches the closed form to `8×10⁻¹⁰`, with the TT rows at exactly `−0.5` (nonzero ⇒ genuine spin-2 mass, not a trace/conformal term). **[B] bare, it is as fatal as the tetrad's** — `\|ε\| = 1.62` per site, order unity. **[C] the self-sustained vacuum removes it** — what gravitates is the grand potential `ρ_Λ = ε − μn = −P`, which cancels to the available floating-point precision across `ε₀ = 1…10¹²²` (largest rows cancel *exactly* in double precision, so the residual is **bounded by**, not equal to, zero). **[D] control that is not a trivial zero** — a **rigid** vacuum that cannot self-adjust retains `\|ρ_Λ\|/ε₀ = 0.75` at every scale, so the equilibrium condition is doing real work. **[E] against §8.26's bound** — residual `m ≈ 2×10⁻⁸` of the bare scale vs the `~3×10⁻⁵` that would destroy the attractor: **~1000× below it. The attractor survives its own decisive test.** **Honest ceiling:** this is protection by an **equilibrium condition, not a symmetry** — exact at equilibrium, but a self-sustained vacuum is a *dynamical state* rather than a redundancy of description, so it is weaker than the photon's, which no dynamics can spoil. It is also **not independent evidence**: same Volovik mechanism as §8.13, so the two stand or fall together |
| `test_graviton_transversality.py` | **Is the induced graviton action diffeomorphism invariant? Weinberg's first factor, measured — and an honest negative for the tetrad.** `γ=1` has never been measured in this project; it rests on Weinberg's theorem, which needs a massless spin-2 **and** a quadratic action invariant under `h_ij → h_ij + ∂_iξ_j + ∂_jξ_i`. The project worked hard on the first and always **assumed** the second. `test_graviton_ward` couldn't measure it (cutoff surface term broke even the photon validation); `test_lattice_ward` then declared it structurally unmeasurable, naming two obstructions: **(i)** the identity is *inhomogeneous* because `⟨T^ij⟩≠0`, **(ii)** diffeomorphism invariance isn't a lattice symmetry. **Obstruction (i) was already dead and nobody noticed** — that `⟨T^ij⟩` is the *same object* as the `q=0` graviton mass, which §8.28 identified as the cosmological constant and cancelled at self-sustained equilibrium. So the question is reopened, with a **nonperturbative** instrument: the finite-`q` filled-sea energy, which contains **every seagull at once** (both earlier attempts used bubbles — and `test_lattice_ward`'s *graviton* bubble had **no seagull at all** while its photon bubble did, so the two were never like-for-like). **Calibrated twice before use: [A]** reproduces §8.27's tetrad mass `−0.2746043896` to **10 digits** by an independent code path; **[B]** an **exact finite-`q` photon Ward identity** — pure-gauge response at the round-off floor (`2.2×10⁻¹¹`) against a nonzero transverse response (`6.4×10⁻⁴`), ratio `3×10⁻⁸` — precisely the calibration `test_graviton_ward` lacked. **[C]** the graviton's pure-gauge response is nonzero, but mostly the `q=0` mass riding along. **[D] with that removed (§8.28's equilibrium), the violation survives — and the ratios are flat in `q`.** A diffeo-violating operator holding a fixed fraction of the invariant one is **marginal, not irrelevant**: by §8.26's own criterion it never flows away, so diffeomorphism invariance is **not emerging in the infrared**. **[E] it isn't even rotationally invariant** — `h₊` and `h×` are both TT for `q` along `z` and must be degenerate; they split by a **converged 12.4%**, flat in `q` *and* in the transverse grid from `Nperp=20→56` (checked because that grid is symmetric under x↔y but not under 45°, and could have manufactured the effect). **[F]** fitting the four two-derivative invariants against linearised Einstein–Hilbert's unique `(1,−2,2,−1)` gives **`(1, +8.18, −0.56, −0.12)`** — not close, and the second coefficient has the **wrong sign**; residual `3.6×10⁻²` because the cubic anisotropy isn't representable in a rotationally invariant basis at all. **Verdict: Einstein structure cannot be inherited from the tetrad** — measured, not inferred. **Scope, stated up front:** this is the **tetrad** sector, which the project already knows isn't its gravity (§8.27 unprotected, `test_light_bending` inert). The **deconfined curvature sector is untouched**. The door §8.27 left ajar is now shut in both directions — the tetrad supplies neither masslessness nor Einstein structure — and `γ=1` still rests on Weinberg applied to the curvature sector, which **remains the open problem** |
| `test_graviton_nullspace.py` | **The induced graviton form has no gauge null space — why projection cannot rescue γ=1.** `test_graviton_transversality` left one hope: the model's gravity is the deconfined **curvature** sector (incompatible strain), so maybe the diffeo violation lives entirely in a gauge subspace a projection discards. That hope is precise — it asks whether the induced quadratic form is **block-diagonal** between physical and gauge modes — and measurable. Measured the **full 6×6** form on symmetric `h_ij` at fixed `q` (every diagonal *and* cross term, orthonormal basis, `q=0` mass removed). **Prediction stated first:** linearised diffeo invariance = a **rank-3 null space** (one zero per `ξ` component); Einstein–Hilbert's own 6×6 has exactly that kernel. **[A/B] The induced form has NO kernel** — all six eigenvalues nonzero `[0.65, 0.81, 0.93, 3.97, 3.97, 7.60]×10⁻³` vs EH's `[−1,0,0,0,1,1]` — and the gauge directions are its **stiffest** modes (4–8× the physical ones), not its flat ones. **[C] The mixing kills projection:** the physical↔gauge off-diagonal block is **42% of the physical block itself** (EH: exactly 0), concentrated in the **spin-0 sector** (transverse-trace coupled to the longitudinal gauge mode) — exactly where `Ψ`, hence `γ`, is set for a spherical source. *You cannot project out a mode that feeds the one you keep.* **[D]** The spin-2 doublet fails independently via §8.29's **12.6%** anisotropy split, which no projection onto incompatible strain can reach (spin-2 can't mix with spin-1 gauge modes). **Verdict: the §8.29 negative is structural, not a near miss** — the induced tetrad action is not a deformation of EH in *any* subspace. The tetrad route to `γ=1` is now **fully closed**; it rests entirely on the curvature sector, measured directly |
| `test_deser_bootstrap.py` | **The nonlinear self-coupling was never free — Deser's bootstrap fixes it, and §8.22 had it wrong.** §8.22 gave the field a cubic self-coupling (the term that makes gravity gravitate) and **swept its strength `λ` as a free parameter** (`λ=0,0.4,0.8,1.6,200` at `g=6`). It is not free. **Deser's bootstrap:** a spin-2 field coupled to matter's stress tensor is *inconsistent* unless it couples to its **own** stress tensor at the **same** strength (matter's stress stops being conserved once the field reacts back); iterating rebuilds Einstein–Hilbert. The project's own Hamiltonian already writes both terms side by side — matter as `(g/2)h_ab S_ab`, field as `(λ/2)h_ab ∂_a h_ij ∂_b h_ij` — so **`λ/g` is a Nordtvedt parameter** (how strongly gravitational binding energy gravitates vs ordinary energy), and the strong equivalence principle fixes **`λ=g`**. **[A] Identification verified independently** — that `∂_a h ∂_b h` is the field's stress tensor is checked by the field energy's response to a **coordinate deformation** (the §8.27–8.28 method, never referencing the contraction): agrees on all six components, shears included, to **`10⁻¹⁰`**. **[B]** §8.22's five values are `λ/g = 0, 0.067, 0.13, 0.27, 33.3` — **none is 1**; the headline case overcouples binding energy **33×**. **[C]** Redone at `λ=g`: the self-interaction shifts the radiated energy by **0.06%**, not the **2.0%** §8.22 reported at `λ=200`; the budget still closes to `3.5×10⁻¹¹`. **Nothing in the integration was wrong — the parameter was.** **[D]** With `λ=g` the breakdown scale is `h~1/g` (the Planck scale in §8.24's `g²=32πG` normalisation), consistent with §8.19. **Scope:** the bootstrap fixes the *cubic* term **given** a Fierz–Pauli *quadratic* term — which §8.20–8.24's postulated field has by construction (it earned its keep on the quadrupole law and Peters inspiral). It does **not** connect that field to the **induced** action of §8.29, which isn't Fierz–Pauli. That gap is the central open problem |
| `test_gamma_source.py` | **Does mass source curvature? The smooth coupling behind γ=1 — measured, and it's zero.** `test_einstein_source` reduced light-bending to one number: **γ = κ/(4πG)**, where 4πG sources the Newtonian time potential Φ and κ sources the spatial curvature Ψ. Φ is measured and healthy; the question is κ. A static point mass is a localized energy density, so it sources gravity **only** through `T⁰⁰` — and any curvature it makes must come from the induced coupling of that energy density to the spatial stress, `Π⁰⁰’ⁱʲ(q)`. **The selection rule:** that coupling **vanishes identically** — `⟨T00, T_ij⟩ = 0` for every component *including the trace*, to machine precision (`~10⁻¹⁹`) at every `q`, mass, and cutoff — while the Newtonian `⟨T00, T00⟩` is everywhere nonzero. It's a *symmetry zero*, not an unresolved small number: the energy-density vertex is `E·I` (a **scalar**), and a scalar cannot source the **spin-2** spatial stress. **The contrast proves it's real:** a genuine spin-2 source couples to the same stress at **O(1)** (`1.03`) — the bubble is fully capable of a nonzero answer; the scalar mass just doesn't elicit one. **So through the smooth loop a mass sources Φ but not Ψ: γ_smooth = 0.** This is the **fourth independent** closure of the smooth route to γ=1 — with the non-dynamical induced spatial graviton (`test_induced_sign`), the non-Einstein induced tetrad action (`test_graviton_transversality`), and its absent gauge null space (`test_graviton_nullspace`) — and pure elasticity gave the *same* zero from the other side (`test_einstein_source`: mass → compatible displacement). **Every smooth mechanism the model has returns γ=0.** **Scope (a negative, stated carefully):** this is the **smooth** channel. Real curvature here is **incompatible strain — disclination density** (`test_light_bending`, `test_disclination_force`), which a smooth stress-stress bubble cannot see. Whether a mass sources curvature through that **topological** channel is untouched and is the remaining, harder measurement. As it stands **γ=1 rests entirely on the emergent-Weinberg argument** (massless spin-2 on a conserved IR stress tensor → Einstein) — which, unlike the model's emergent Lorentz invariance, has **no direct in-model confirmation**, and now has **direct evidence against it** in the smooth sector |
| `test_gamma_topological.py` | **The topological channel behind γ=1 — does mass carry net curvature charge? Gauss–Bonnet says no.** §8.32's selection rule only forbids a *scalar* mass from sourcing the *spin-2* spatial stress. But **curvature is itself a scalar** (the Ricci scalar / incompatibility η), and `test_incompatible_gravity` located a genuine scalar curvature DOF in the medium (the 3rd bond mode). A scalar mass sourcing scalar curvature is **not** spin-forbidden — so this is the one route left, and it runs through the model's **own** gravity mechanism: **compression**. Field energy changes the medium's preferred density = an **eigenstrain** `θ*(x) ~ ρ(x)`, the non-uniform thermal expansion that buckles a heated plate. Unlike a body force (→ compatible displacement, η=0), an inhomogeneous eigenstrain leaves **real** Gaussian curvature `η = ∇²θ*` that light on the bond metric must see. **[A]** it's nonzero locally (`max\|η\|/max\|θ*\| = 0.22`) — mass really does curve the medium. **[B] but its net curvature charge vanishes — Gauss–Bonnet.** `∫η = ∫∇²θ*` is a total derivative, **exactly zero** (`~10⁻¹⁵`) for every profile width: a smooth compression makes a curvature **dipole** — dome in the core, saddle in the ring — with no net deficit angle. **[C]** zero net charge ⇒ **zero long-range bending** (`\|α\| = 0` at every impact parameter), against a genuine curvature charge `η~ρ` that bends light and stays long-range (`12.96 → 8.25`). **So compression gives γ=0.** **[D] The complete picture: every accessible direct channel gives γ=0**, each for its own clean reason — spin selection rule (`test_gamma_source`), strain compatibility (`test_einstein_source`), Gauss–Bonnet (here). γ=1 needs mass to carry **net curvature charge** = nucleate a **disclination density ∝ ρ**, which no sector supplies. **⚠️ Honest dimensional caveat:** Gauss–Bonnet is a **2D** theorem and these probes are 2D / 2+1D — the settings where the incompatible-curvature picture lives. In **3+1D** GR a smooth mass sources smooth Ricci curvature with *no* topological defect (= γ=1), so whether the 3+1D medium evades this is a *further calculation not done here*. **Net:** the model's gravity as directly realized couples to mass as a **scalar** (`T⁰⁰` alone) → **Nordström, γ=0**; γ=1 rests entirely on **emergent-Weinberg** (IR-emergent, like the graviton Ward identity), which — unlike directly-confirmed emergent Lorentz — has no positive in-model evidence and now a consistent set of direct results against it. **Newtonian gravity is real and healthy; the Einstein completion is argued, not demonstrated.** |
| `test_gamma_3d.py` | **The 3+1D answer: does the medium reach γ=1 like GR? No — and the reason is deeper than Gauss–Bonnet.** §8.33 left one honest opening: Gauss–Bonnet is a **2D** theorem, and in 3+1D a smooth mass sources smooth Ricci curvature with no topological defect (= γ=1). This settles it in **genuine 3D by direct ray tracing** — the medium does **not** reach γ=1, for a dimension-independent reason. **GR sources the spatial metric by a Poisson equation**, `∇²Ψ = 4πGρ`, so Ψ is the **long-range potential** — the same 1/r that carries Φ — giving `γ = Ψ/Φ = 1`. **The medium doesn't solve a Poisson equation for its spatial metric.** Its gravity is compression: mass = eigenstrain `θ* ~ ρ`, and the **3D incompatibility of an isotropic eigenstrain is `δ_ij∇²θ* − ∂_i∂_jθ*`** — exactly the linearized Einstein tensor of `h_ij = 2θ*δ_ij` (**verified to `10⁻¹⁶`**) — so the spatial metric follows the compression **algebraically, Ψ = θ* = ρ. That is local**, dying with the mass, not the potential. **[B] Ray-traced in 3D:** a **GR control** (Ψ = potential) holds `γ = 1` at every impact parameter — validating the tracer — while the medium's `γ` (Ψ = ρ) **falls to zero with distance** (`−0.14 → −0.07 → −0.013 → −0.0002 → 0`). **[C]** robust to a few parts in `10⁴` at far field across grid/box/width. **[D] The reason isolated:** `Ψ_medium ~ ρ` (local, dies fast) vs `Ψ_GR·b ~ const` (i.e. 1/r). **The 2D Gauss–Bonnet zero was a symptom; the disease is the absence of a Poisson equation for the spatial metric.** **This finishes the direct search:** every channel in every accessible setting gives **γ=0** — spin rule (`test_gamma_source`), compatibility (`test_einstein_source`), 2D Gauss–Bonnet (`test_gamma_topological`), 3D local compression (here). γ=1 needs mass to source the graviton's **spatial** polarizations as a Poisson potential — precisely the coupling §8.32 measured to vanish. The graviton is massless and healthy (`test_spin2_dynamical`), but **mass doesn't couple to its spatial modes — only to the scalar compression.** The emergent-Weinberg **premise** (mass couples to the full conserved `T_μν`) is what fails, directly, in 3D. **Bottom line: Newtonian gravity is real, healthy and quantitative (the quadrupole law and Peters inspiral stand); the Einstein completion — γ=1, the factor of two — is not realized by any direct mechanism in any dimension, and is argued, not demonstrated.** |
| `test_gamma_elastic.py` | **The last route: can the medium's Poisson ratio make the graviton propagator Fierz–Pauli? No.** §8.32–8.34 closed every way a mass could *source* curvature. One route remained at the **propagator** level: γ=1 also follows if the graviton kinetic term is **Fierz–Pauli** (a `T₀₀` source then produces spatial curvature through the propagator's trace structure). §8.29–8.30 ruled that out for the fermion-**induced** tetrad, but the deconfined curvature sector's kinetic term is the **medium's own biharmonic elasticity** — whose trace structure is set by its **Poisson ratio** — and that was never measured. This is the classic elasticity-as-gravity question (a special ν could in principle give Fierz–Pauli). **[A]** Measured by straining the LJ medium: the condensate is a **central-force Cauchy solid, ν ≈ 1/3** (`C₁₂/C₁₁ = 0.330`). **[B] The decisive test:** the medium's response splits into an elastic **relaxation** (ν-dependent) plus the imposed **eigenstrain**. A relaxation is a *displacement*, so its strain is **compatible → curvature identically zero** — verified machine-zero (`~10⁻⁸` vs the eigenstrain's `0.667`) for **every ν from −0.9 to 0.49**. The Poisson ratio tunes only how the *flat* displacement is distributed. **[C] γ(ν) ray-traced:** **identical for every ν** (the light-bending curvature is the incompatible eigenstrain part, which no ν touches) → the zero of §8.34. **[D] In Fierz–Pauli terms:** the elastic constants multiply only the **compatible (gauge) sector**, which drops out of the gauge-invariant curvature — so tuning them can't move the graviton's trace structure toward Fierz–Pauli. **Verdict: the propagator route is closed, exactly as §8.29–8.30 closed the induced-tetrad one.** **Every route to γ=1 is now shut** — neither the **source** (spin rule §8.32, Gauss–Bonnet §8.33, local compression §8.34; every channel and dimension) nor the **propagator** (§8.29–8.30, §8.35) gives it. The graviton is massless and healthy (`test_spin2_dynamical`), but a static mass couples to **none** of its spatial polarizations by any mechanism the medium provides. **γ=1 is not recoverable by any property of the medium the model can adjust** — it survives only as the emergent-Weinberg argument, whose premise these measurements contradict. Newtonian gravity real, healthy, quantitative; the Einstein completion argued, not demonstrated. |
| `test_gamma_sublattice.py` | **The negative-space sublattice: does a second (bcc-void / interpenetrating) lattice supply the missing Fierz–Pauli term? No — but it targets the one real assumption.** Every γ=0 result, §8.35 included, rode on a hidden premise: the medium is a **single Bravais lattice with central forces**, which forces the Cauchy relations *and* makes the light-seen strain the gradient of **one** displacement `u` (Saint-Venant → compatible → flat). The **fcc + negative-space (bcc-like) two-sublattice** medium breaks that: it has a **relative (optical) mode** `w = u_A − u_B` that is not `∇u` — textbook, since two interpenetrating fcc = **diamond**, which violates Cauchy through this internal-strain (Kleinman) mode. Measured in the clean 2D proxy (honeycomb = two triangular sublattices). **[A] The fork — is the optical mode gapped or gapless?** Its `q=0` gap `∝ k_inter` (`ω_opt = 0.87, 1.73, 3.46` for `k_inter = 0.25, 1, 4`): a mechanical second sublattice's relative mode is **gapped**, so it is adiabatically **slaved** to the strain — it renormalises the elastic constants but is *not* an independent long-range field. Only a **gapless** relative mode (a Goldstone of a *second condensate*) would be one. **[B] Cauchy is genuinely broken:** relaxing the internal mode gives `C₁₂−C₆₆ = +0.29 ≠ 0` (frozen: `5×10⁻⁷`), while leaving the **compression modulus `K` invariant to 14 digits** and softening only shear — so §8.35's premise really is escaped, but the optical mode couples to **shear, not compression**. **[B2] The selection rule, exact:** the slaved internal shift `s(ε)` is `‖s‖=4×10⁻¹⁰` (machine zero) for an **isotropic** strain, `1.0` for **shear** — an isotropic mass sources no internal shift; the 3 bond forces on B cancel by 3-fold symmetry. **[C] Decisive:** the **full** two-sublattice response to a mass (macro `u` + slaved `s`) has curvature (incompatibility / Ricci) **`8×10⁻¹¹` — machine zero** — against a live probe (raw eigenstrain `θ*δ`) at `0.125`. Because the optical mode is slaved, `s` is itself a **displacement**, so the total strain is a symmetric gradient → **compatible → flat**. **Verdict: the second sublattice adds a second *displacement*, not an *incompatible* degree of freedom** — so it cannot escape the compatibility trap of §8.34/8.35. It would help only if (i) the relative mode were **gapless** *and* (ii) a mass could source its **shear** channel — which the isotropy of a point mass forbids ([B2]); even the crystallographic 3θ wiggle it does make carries **zero net disclination** (Gauss–Bonnet, §8.33). γ=1 is not reachable by adding the negative space; the Newtonian (scalar) sector remains the model's honest gravity. |
| `test_gamma_trace.py` | **The whole γ arc in one number — the graviton propagator's trace coefficient — and why it doesn't flow to Einstein in the IR the way Lorentz does.** A *synthesis*, not a new probe: §8.29–8.36 measured γ≠1 from every side; this names the single quantity it all lives in and settles the question the arc was really about. **The reduction:** linearized gravity sends a static source to a metric via the graviton propagator, whose only free piece (massless spin-2) is the trace-term coefficient **λ**. With `ds² = −(1+2Φ)dt² + (1−2Ψ)δ_ij dxⁱdxʲ`, a source `diag(ρ,p,p,p)` gives `γ = Ψ/Φ = [p+λ(ρ−3p)]/[ρ−λ(ρ−3p)]`. Fierz–Pauli/Einstein is **λ=½**. **[A]** For a realistic **pressureless** light-bending source (the Sun, `p/ρ~10⁻⁵`), this collapses to **`γ = λ/(1−λ)`** — verified numerically: `λ=½→γ=1` (GR), `λ=0→γ=0` (Nordström). **The factor of two IS the trace term.** **[B] Pressure is a red herring:** at `p/ρ~10⁻⁵` pressure moves γ by `~10⁻⁵`; real light-bending sources are dust, so γ is fixed by λ alone — a source's pressure cannot substitute for the propagator's trace term. **[C] The model's measured λ, both sides → 0:** the **source** coupling `⟨T00,T_ij⟩=0` (§8.32) is exactly *no trace term* (λ=0 → γ=0); the **induced propagator** trace coefficients `(1,+8.18,−0.56,−0.12)` vs EH `(1,−2,2,−1)` (§8.29) are wrong by O(1), second **wrong sign** — λ nowhere near ½. **[D] The decisive point — the RG fate.** γ=1 and emergent Lorentz are the *same kind of statement* (a continuum symmetry the lattice breaks), so does γ **emerge** in the IR as Lorentz demonstrably does? Opposite answers, for one reason: Lorentz's cone anisotropy is RG-**irrelevant** (`~(k/k_Planck)²→0`), so it flows away and Lorentz emerges; the graviton's diffeomorphism violation is RG-**marginal** (§8.29's flat ratios `1.07, 4.06` unchanged as `q→0`, converged `12.4%` anisotropy), so it holds a fixed fraction and **γ stays pinned off 1 at every scale**. Side-by-side γ(scale): irrelevant → `0→1`; marginal → flat `≈−0.23`. **Verdict: γ=1 fails not for want of measurement but because the one coefficient that would deliver it is marginal, not irrelevant — no continuum limit of *this* lattice recovers it.** The model earns emergent SR, fermions, most of QM, EM, and a real healthy **Newtonian** gravity; the Einstein factor of two would need a different (background-independent) construction, not a refinement of this one. |
| `test_photon_width.py` | **Does the photon's *width* bend light? Testing whether the zero-width ray hid the factor of two — it didn't.** Every γ measurement here ray-traced light as a zero-width geodesic, so "light is a thin ray" is a real buried premise; this tests it by giving the photon a finite transverse width. **The physics:** the factor of two is not a property of the photon but of the **geometry** — deflection is `(1+γ)×`Newton, the "1" the time warp `g₀₀` (a scalar index `n=1−Φ`), the "γ" the **spatial** curvature Ψ (`n` gains `−Ψ`). Light samples *both, equally,* because it moves at **c** — on a null path `dx = c·dt`, so space and time enter with equal weight. That null-ness, not width, is the origin of the factor of two; the model already has null light (emergent Lorentz ✓) but sources no Ψ. **[A]** Zero-width ray: model index `n=1−Φ` → `α=0.497` (γ=0), GR index `n=1−2Φ` → `α=0.994` (γ=1), ratio **exactly 2** — the factor of two lives in the index. **[B]** A finite-width photon (centroid deflection, exact via Ehrenfest `d⟨k_x⟩/dz = −⟨∂ₓV⟩_beam`) through the **model** index stays at the ray value — implied γ `0.0004 → 0.15` across `w/b = 0.02 → 0.33`, only an **`O((w/b)²)` tidal correction** from averaging `∇n` over the profile. **[C] Control:** the same width photon through a **GR** index reads implied γ `1.00 → 1.31` — the width average *sees* the factor of two when the geometry has it, so [B]'s null is physics, not the ray picture hiding something (diffraction, omitted, only spreads the profile further and washes width effects out more — it cannot add the factor of two). **Verdict: width multiplies whatever γ the geometry carries; it never creates it.** A wide photon and a thin ray both bend by `(1+γ)×`; the model's γ=0 is robust to giving the photon any width, because a mass sources no Ψ for a probe of *any* size to feel. Einstein's factor of two is about light's **speed**, not its **width** — the model has the speed, not the spatial curvature. |
| `test_lv_confrontation.py` | **Testing the model's Lorentz violation against real data — and it's *closer* to the frontier than we'd said.** The model's one falsifiable empirical claim: quadratic (n=2, mass-dim-6) LV, `v(E)/c = 1 − 0.245(E/E_Planck)²`, **subluminal**, **species-universal** (one cone: photon = electron = graviton at leading order), with a crystallographic anisotropy. `test_lv_prediction` confronted it and said "safe by many orders" — but that compared the model's `E_QG` against the UHECR *energy* (10¹¹ GeV) as if that were the *bound*. It isn't. **[B] Done right:** for n=2 the effect grows as `E²`, so the strongest bounds come from UHE astrophysics and sit near the **Planck scale** — UHECR/GZK `E_QG,2 ≳ 10¹⁸ GeV`, Crab electrons `~5×10¹⁷`, PeV-photon non-decay `~10¹⁹`. The model predicts `E_QG,2 ≈ 2.5×10¹⁹ GeV` (~2× Planck), so its effect is **`~10⁻³` of current UHECR sensitivity — ~25× in `E_QG`, ~1.4 orders in reach, not 16.** The n=2 frontier is near Planck and *so is the model*: this is the closest its predictions come to falsification, and plausibly reachable by next-gen UHE observatories. **[C] A prediction it has already PASSED:** one universal cone → `c_γ = c_grav = c_ν` at leading order, confirmed by **GW170817** (light & gravity within ~1s over 130 Mly, `‖Δc‖/c < 3×10⁻¹⁵`) and neutrino multimessenger timing. Many LV scenarios predict species-dependence and are constrained; the model forbids it (one-structure/Volovik) and the data agree. **[D]** The distinctive crystallographic anisotropy is a real preferred-frame/axes effect, but doubly suppressed (`~10⁻²⁰` at UHE, `~10⁻⁶⁰` in lab cavities) and likely **averaged away** in a poly-domained medium — so **one-cone universality, not the cubic pattern, is the real discriminator.** **[E] Kill conditions** (no Planck access needed): a confirmed **linear** (n=1) photon LV (model is strictly n=2); a confirmed **species-dependent** speed (one cone forbids it; GW170817 already tests it, passed); a **superluminal** photon (model is subluminal). **The model's real experimental target is ultra-high-energy astrophysics** — not lab cavities or linear-LV GRB searches. |
| `test_vacuum_gravitates.py` | **Does the vacuum energy gravitate? Verifying the CC solution — and finding it's the *same fact* as γ=0.** `test_cosmological_constant` dissolved the 10¹²² fine-tuning via Volovik: the gravitating vacuum energy is the grand potential `ρ_Λ = −P`, and a self-sustained condensate has `P=0`, so `ρ_Λ=0` automatically. But its own note flags the load-bearing piece as *assumed*: "the emergent metric couples to the stress `−P`, not the energy density `ε`" — which is exactly the **tension** with the γ arc (gravity couples to `T₀₀`). This verifies it, by sector. **[A] On the real medium** (self-bound LJ condensate, edge-free lattice sum): energy density `ε = −3.16` scaling to `−3×10⁶`, while pressure `P(a₀) = −4×10⁻¹¹` — **machine zero at equilibrium**, and self-restored to zero after a "phase transition" that shifts the preferred spacing. A huge energy density coexists with an exactly zero cosmological constant, self-tuned, for any condensation depth. **[B] The verification** (induced-gravity bubble): energy density sources the Newtonian `h₀₀` (`⟨T00 T00⟩ = +0.16 ≠ 0`) but sources **no** spatial curvature (`⟨T00, T_ij⟩ = 7×10⁻²⁰ = 0`, a symmetry zero), while spatial stress *does* couple to the spatial metric (`⟨T₊ T₊⟩ = +0.51 ≠ 0`). So the Volovik assumption is **measured, not assumed**: the vacuum's `ε` gravitates only as an unobservable uniform `h₀₀`; the CC is its pressure `P`, which self-tunes to zero. This reconciles the tension — "gravity couples to `T₀₀`" (γ arc, the `h₀₀` sector) and "the gravitating vacuum energy is `−P`" (the CC, the `h_ij` sector) are **both** correct, and both follow from `⟨T00,T_ij⟩=0`. **[C] The unification:** that one selection rule has two consequences — **γ=0** (a mass's energy density doesn't bend light) *and* **Λ≈0** (the vacuum's energy density doesn't drive expansion). **The model doesn't reach Einstein gravity for the very reason it has no cosmological-constant problem — the factor of two and the 10¹²² are one fact.** A theory that bent light like GR would carry the 10¹²² problem; this one has neither, together. **Scope:** makes the *equilibrium* Λ exactly zero and explains why; the observed nonzero Λ remains the off-equilibrium (expansion/matter/relaxation) question. |
| `test_cc_weinberg.py` | **Does the zero cosmological constant survive Weinberg's no-go? Yes — the self-tuning is a flat direction.** The reason dynamical Λ-relaxation almost always fails is **Weinberg's theorem**: the field that relaxes Λ to zero generically also shifts observable constants, so you can't keep both. The self-adjusting field here is the medium's own **scale** (density/spacing/energy-depth), so this stress-tests exactly that. **[A] Robustness:** a "phase transition" that changes the vacuum energy density over **~8 orders** (`−3.16 → −2.4×10⁸`) moves the medium to a new equilibrium where `P(a₀) = 0` **every time** — so Λ stays zero across each transition automatically, whereas in GR each transition's vacuum energy gravitates and needs a fresh counterterm. **[B] The Weinberg cost, measured:** the emergent phonon/cone spectrum across those vacua — its **dimensionless** observables (`c_L/c_T = 1.7330310`, `ω_M^hi/ω_M^lo = 1.2987878`, `ω_M^hi/c_L = 0.6869502`) are **invariant to ~8 digits** while the vacuum energy spans orders. Absorbing a uniform vacuum-energy change is a **pure dilatation** — all energies and lengths rescale together — so no observable constant moves. The medium's overall scale is a flat direction the vacuum energy couples to and physics does not: exactly the loophole Weinberg's theorem requires and generic solutions lack. **[C]** Because the whole spectrum rescales rigidly, all emergent speeds move together — **one-cone universality is preserved**, no differential Lorentz violation induced. **Verdict: keeping Λ=0 costs no observable constant** (for uniform vacuum changes). **Scope:** covers *uniform* (isotropic) transitions, which dilate the medium; a *structurally anisotropic* transition could shift ratios and isn't tested. Still the equilibrium story — the observed nonzero Λ remains the off-equilibrium (expansion/relaxation) question. |
| `test_cc_offequilibrium.py` | **The off-equilibrium Λ: what the expansion lag predicts — and why it isn't the observed dark energy.** The equilibrium mechanism makes Λ *exactly* zero; the observed value is small but *nonzero*, which needs the vacuum slightly off equilibrium. The one physical reason it would: cosmic expansion dilutes the medium faster than it relaxes back. For the density departure `δ`, `δ̇ = −3H − δ/τ`, so `δ_ss = −3Hτ` — expansion holds the medium a fraction `~Hτ` below equilibrium (rarefied → tension → positive, dark-energy-like `ρ_Λ = −P`). **[A]** Measured the real medium's equation of state (bulk modulus `B = dP/dδ = 56.9`). **[B]** The relaxation ODE confirms `δ_ss = −3Hτ`, and `ρ_Λ ∝ H¹·⁰⁰` — a **dynamical** dark energy (not a constant). **[C] Magnitude, honest:** at Planck-scale medium parameters (`B ~ M_P⁴`, `τ ~ t_P`), `ρ_Λ ~ M_P³H`, so `ρ_Λ/M_P⁴ ~ 3H₀/M_P ~ 4×10⁻⁶¹` — versus observed `~10⁻¹²²`, still **~61 orders too big.** This is the **geometric mean** of the problem: the equilibrium mechanism removed all 122 orders (Λ=0 exactly); the expansion lag puts `√` of them — ~61 — back. **[D] Equation of state:** `ρ_Λ ∝ H` gives an evolving `w ≈ −½` in the matter era (`→ −1` in de Sitter) — a real signature, but in tension with the observed `w ≈ −1`. **Verdict — the honest boundary of the CC solution:** the model settles the *equilibrium* CC (exactly zero, self-tuned, Weinberg-robust); the observed *nonzero* Λ is **not** derived — the simplest off-equilibrium source (expansion lag) is both too large *and* the wrong `w`, so the dark energy is likely a different, smaller departure. Why the universe sits so extraordinarily near the medium's equilibrium is where this thread relocates the coincidence — not where it closes it. |
| `test_cc_attractor.py` | **Why so near equilibrium? An attractor, an aging universe — and `ρ_Λ ~ ρ_crit` if the medium tracks the expansion.** The off-equilibrium gap: the observed Λ needs the vacuum `~10⁻¹²²` off equilibrium; the naive lag gives `~10⁻⁶¹`. This pushes on *why* the universe sits so near equilibrium. **[A] Attractor (solid):** `δ̇ = −3H − δ/τ` has homogeneous part `δ ~ e^(−t/τ)`, so *any* initial departure — any initial vacuum energy — decays to the same attractor in a few `τ ~ t_Planck`. Verified: `δ₀ ∈ {−0.5 … +0.8}` all converge to `δ_ss = −3Hτ` within `~10τ`. **There is no initial Λ to fine-tune** — the medium forgets it in a Planck time; today's Λ is the attractor, not a relic. The initial-conditions version of the CC problem doesn't arise. **[B] Cosmic age (solid):** the attractor tracks H, so Λ **decays as the universe ages** and is small *now* because the universe is old (`H ≪ M_P`). The smallness of Λ *is* the smallness of `H/M_P`. **[C] The coincidence (conditional — the new lead):** the passive attractor gives `ρ_Λ/M_P⁴ ~ H/M_P ~ 10⁻⁶¹` (one power, `2×10⁶³×` too big). But a medium that **creates nodes to track the expansion** cancels the linear dilution `−3H`; its residual is the **acceleration lag**, `δ ~ (Hτ)²` (second order) — giving `ρ_Λ ~ M_P²H² = ρ_crit`. At `H₀`: `ρ_Λ/M_P⁴ ~ 2.5×10⁻¹²⁰` — **within ~2 orders of the observed `10⁻¹²²`** (the residual being the order-unity coefficient `B ≈ 57`). The whole 61-order gap is the *second* power of `H/M_P`. **This is the first mechanism in the arc to reach `10⁻¹²²`** — and it explains the observed `ρ_Λ ~ ρ_crit` coincidence. **Honest scope:** conditional on efficient tracking (assumed, not derived) and fixing only the *scaling* (order-unity coefficient open); it predicts **dynamical dark energy tracking the critical density**, to confront with evolving-`w` data. |
| `test_cc_tracking.py` | **Deriving the tracking efficiency — and the `(Hτ)²` dark-energy lead does *not* survive it.** `test_cc_attractor`'s `[C]` floated a conditional lead: *if* the medium tracks cosmic expansion, the residual is the acceleration lag `δ ~ (Hτ)²` → `ρ_Λ ~ ρ_crit` (observed). It hinged entirely on the tracking efficiency `ε`, left underived. This derives it. **[A] The efficiency sets the power:** `δ_ss = −3(1−ε)Hτ`, so `ρ_Λ ~ B(1−ε)H`. Only a shortfall `1−ε = Hτ` (a fine-tuned marginal tracking) lands near `10⁻¹²²`; `ε=0` (no tracking) overshoots by `2×10⁶³`, `ε=1` (perfect growth) gives exactly zero. **[B] What the medium actually does:** node creation in a condensate is driven by the **chemical potential** — the tension `P = Bδ`, a *state* (deficit) variable, measured (`P/δ = B = 56.9 > 0`). The medium senses its own local density, **not** the global Hubble rate `H`. A deficit-driven creation fires only once the medium is *already* rarefied → **`ε = 0`**. **[C] Therefore** `δ ~ −3Hτ` (linear), `ρ_Λ ~ 10⁻⁶¹` — 61 orders too big; the `(Hτ)²` lead required `ε = 1−Hτ` (marginal rate-tracking) the medium doesn't provide. The alternative — perfect growth at equilibrium — is `ε=1` and gives `ρ_Λ = 0`. **Neither natural mechanism lands on `10⁻¹²²`.** **Verdict:** the tracking lead was too optimistic; derived, the efficiency gives `10⁻⁶¹` or `0`, bracketing but not hitting the observed value — the coincidence is *not* explained by node tracking. **What stands (unchanged):** the *equilibrium* CC is exactly zero, self-tuned, Weinberg-robust; initial conditions are erased by the attractor in a Planck time. The observed *nonzero* Λ remains underived — the honest boundary of the CC solution, now confirmed against the one mechanism that looked like it might cross it. |
| `test_electroweak.py` | **The electroweak Higgs mechanism: the medium's condensate breaks `SU(2)×U(1) → U(1)_EM`.** The gauge *mechanism* exists (Yang-Mills induced, `test_yang_mills`) and the Abelian Higgs was measured (Meissner mass, screening arc); this marries them into the electroweak sector. The condensate — the same amplitude mode that gaps for gravity and self-tunes the CC — sits in a **doublet** (fundamental of `SU(2)_L`, hypercharge `Y=½`); its VEV `⟨Φ⟩=(0,v/√2)` breaks the induced `SU(2)×U(1)`. From the covariant derivative `‖D_μΦ‖²`, the 4×4 gauge-boson mass matrix. **[A] Spectrum:** diagonalized `= {0, m_W, m_W, m_Z}` — **exactly one massless photon and three massive** (`W±, Z`), for any couplings, with `m_W = gv/2` and `m_Z = √(g²+g'²)v/2` exact (photon machine-zero). The `SU(2)×U(1)→U(1)` signature — not zero massless, not two. **[B] Weak mixing angle:** `m_W/m_Z = cos θ_W = g/√(g²+g'²)` verified across `g'/g` (e.g. `sin²θ_W = 0.223` at the physical point) — the mixing angle is the geometry of the W³–B mixing the condensate induces, not a separate input. **[C] Why exactly one photon:** the unbroken generator is the electric charge `Q = T₃ + Y`, and the vacuum is **Q-neutral** (`Q⟨Φ⟩ = 0`), so the photon stays massless while the three orthogonal generators move the vacuum and gain mass — the single surviving photon is forced by the doublet's rep and its `Y=½`. **Verdict:** the electroweak breaking pattern and the W/Z/θ_W mass relations are reproduced, not fitted, by the model's own condensate + induced gauge fields. **Scope (as in `test_yang_mills`):** the mechanism, not a derivation — `SU(2)×U(1)`, the doublet rep, and `Y=½` are inputs; *why* the condensate carries `Y=½` (the assignment leaving EM unbroken, fixed with the fermion charges by anomaly cancellation) is the next question. |
| `test_einstein_source.py` | **Is the coupling Einstein? γ isolated to one number.** `γ=Ψ/Φ` decides light bending (γ=1 = GR's factor of two). [A] Mass sources *no* curvature elastically (`η/ρ = 1.2×10⁻¹⁵`) — equilibrium elasticity returns a displacement, so the model's native gravity (the scalar amplitude mode) is γ=0 (Nordström). [B] Only a topological disclination sources curvature — and it repels. [C] An induced `η=κρ` gives `γ=κ/(4πG)`: γ=1 → bend ×2. So the crossing is **one missing ingredient** — the Einstein source coupling (mass→curvature at γ=1), which the fermion loop must induce by coupling the metric to the *conserved* stress tensor (Weinberg). ✅ **That ingredient has since been supplied:** `μ>0` measured (`test_induced_sign`), the spin-2 graviton is dynamical and healthy in 3+1D (`test_spin2_dynamical`), and γ→1 follows in the IR (`test_lattice_ward`) |
| `test_scale_fixing.py` | **The medium's absolute scale, from data.** Matching the measured `G` with an order-unity coupling forces `a₀ = l_Planck` (an *assumption* in `test_lv_prediction`, now a **consistency result**). And since the model predicts gravity's range is `1/m_A`, every graviton-mass bound measures the medium's distance from criticality: the strongest gives **1 part in 10¹²²** — essentially the cosmological-constant number. Falsifiable: the model predicts a **Yukawa**, not a pure `1/r²`; a measured graviton mass would *measure* the tuning, not kill the model. ⚠️ **The 10¹²² tuning here is RETRACTED by `test_experimental_bounds`:** it assumed gravity's range *is* `1/m_A` (the scalar picture). The tensor arc replaced that — the long-range force is the massless deconfined graviton, so `m_A` faces only a *lower* bound an untuned medium clears by ~30 orders. `a₀ = l_Planck` stands |
| `test_collapse.py` | **Minimum mass to destroy the condensate — and why it's not a black hole.** P1 (mass = confluence of loci) is real: where `2gρ > a` the condensate is locally destroyed. But there is **no critical nucleus and no runaway** — `N(σ)` is monotonic and `N_min` is gap-independent (ratio 0.96 when `a` halves, not the predicted √2), because the condensate is the *true* vacuum so a normal bubble is never favorable. Matter *pins* a suppressed region; remove it and it heals shut. **No gravitational collapse exists in this model** — the third independent reason P2's black hole isn't here (with `ρ_horizon ∝ 1/M²` and scalar gravity not bending light). *An honest self-correction: my critical-nucleus prediction was refuted by the measurement*. ⚠️ **One leg superseded:** "scalar gravity doesn't bend light" no longer holds (gravity is now tensor, γ→1), but the *no-collapse-instability* leg is independent and still carries the conclusion |
| `test_critical_gravity.py` | **✅ WORKING long-range gravity.** The mediator is the condensate's **amplitude mode**: the phase is a Goldstone (shift symmetry ⇒ derivative couplings only ⇒ can never mediate a monopole force), but the amplitude is *unprotected*, so it is gapped — **which is why gravity always looked screened** — and it couples monopolarly to positive-definite energy, so **scalar exchange attracts**. Measured in 3D: **`λ·m_A = 1.00`** (gap read off the potential, not fitted), and dividing out the exponential leaves a pure **`R^-1.00`** core ⇒ `E_int = −C·e^{−R/λ}/R` **exactly**. At criticality: **Newton's law, `1/r²`, universally attractive.** 🟡 Scalar (Nordström) gravity — no light bending, no spin-2 waves. ⚠️ **Superseded in part:** the amplitude mode is no longer read as gravity itself (a long-range scalar forces γ=½, ruled out) but as a *short-range* correction; gravity proper is the spin-2 graviton, and the criticality tuning is **retracted** (`test_two_gravities`, `test_deconfinement`, `test_experimental_bounds`) |
| `test_tetrad_force.py` | **A field falloff is not a force.** Eshelby/**Crum**: two dilatation centres in an infinite *isotropic* medium have **zero** interaction. Measured: the isotropic force **collapses 69×** from `R≈10→22` (only a short-range contact term — the screened Phase-3 attraction). Control (isotropy broken) shows a real long-range force appears, **46×** larger — and **repulsive**. So the tetrad's `1/r²` field exerts **no long-range force** |
| `screening_gauged.py` | Gauged U(1) / Abelian Higgs (G-0/G-1/G-2): gauging the symmetry screens the vortex force (Meissner) — box-independent λ_L ~ 1/e, vs Screen-2's box-growing log |
| `screening_gauged_mobile.py` | Mobile-vortex check: vortices move under the screened force (overdamped, adiabatic gauge); the force law from motion confirms λ_L ~ 1/e |
| `CHEATSHEET.md` | Full reference: equations, parameters, all hypotheses (H1–H10) + integration |

Simulation output goes to `condensate_runs/` (git-ignored); rendered figures to
`figures/` (git-ignored). Personal documents (`*.docx`) are git-ignored.
